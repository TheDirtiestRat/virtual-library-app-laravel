import "./bootstrap";

import jQuery from 'jquery';
window.$ = jQuery;
$ = jQuery; // Optional: Makes jQuery globally accessible as $

// Import other libraries or custom scripts as needed
// Example: import 'some-other-library';
